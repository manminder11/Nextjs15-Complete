const page = () => {
  return (
    <div>
this is a private page       
    </div>
  )
}

export default page


// auth is the folder which is  provoking the straight acess
// by ()puttting the auth in the parenthesis we can have a straight acess to the page (auth)
// mysteriously my (auth) is not visible after putting (auth) in the parenthesis